# Go
/visiorama.exe
/visiorama

# Runtime data
data/
configs/visiorama.yaml

# Frontend
web/app/node_modules/
# dist is intentionally committed — required for Go embed (go build embeds it into binary)

# Vite cache
web/app/.vite/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Claude Code — machine-local permissions (paths are host-specific)
.claude/settings.local.json
